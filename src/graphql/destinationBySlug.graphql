query DestinationBySlug($slug: String!) {
    destinationBySlug(slug: $slug) {
        _id
        name
        description
        maxSlots
        slotsLeft
        slug
        imageUrl
        province  {
            provDesc
            region {
                regDesc
            }
        }
    }
}